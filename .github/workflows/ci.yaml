name: Common CI
on: [workflow_call]
jobs:
  Build:
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo Build

  Test:
    runs-on: self-hosted
    needs: Build
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo Tests

  Deploy:
    runs-on: self-hosted
    needs: Test
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo Tests